# Host-port overrides for docker-compose.yml. 8081 is occupied on this machine by another project's container,
# so Redis Commander is published on 8082 here; delete this line to use the default 8081.
FLOWFORGE_REDIS_COMMANDER_PORT=8082
